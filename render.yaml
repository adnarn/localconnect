services:
  - type: web
    name: localconnect
    runtime: node
    rootDir: localconnect-main
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: JWT_SECRET
        generateValue: true
